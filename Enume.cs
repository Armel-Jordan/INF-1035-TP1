using System;
using System.Collections.Generic;
using System.Text;

namespace TP1
{
    public class Enume
    {
        //CREATION LISTE VALEUR 
        public enum Valeur
        {
            As, deux, trois, quatre, cinq, six, sept, huit, neuf, dix, Valet, Dame, Roi
        }
        // CREATION LISTE COULEUR
        public enum Couleur
        {
            Carreau, Coeur, Pique, Trefle
        }
    }
}
